import React from "react"
import {{lib}} from "{{lib}}"

class {{componentName}} extends React.Component {
    render() {
        return (
            <div>Hi from {{componentName}}</div>
        )
    }
}

export default {{componentName}}