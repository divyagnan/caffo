import React from "react"
import {{lib}} from "{{lib}}"
import "./{{componentName}}.css"

class {{componentName}} extends React.Component {
    render() {
        return (
            <div className="{{rootClass}}">Hi from {{componentName}}</div>
        )
    }
}

export default {{componentName}}